# ============================================================================
# ssvnauka.com Environment Variables
# Copy to .env and set STRONG passwords — do not commit .env!
# ============================================================================

# PostgreSQL (required — no default)
DB_USER=sergiy
DB_PASSWORD=change-to-a-strong-random-password
DB_NAME=ssvnauka

# pgAdmin (required — no default)
PGADMIN_EMAIL=ssvnauka@gmail.com
PGADMIN_PASSWORD=change-to-a-strong-random-password
